name: Run tests

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Go 1.4
        uses: actions/setup-go@v1
        with:
          go-version: 1.14

      - uses: actions/checkout@v1
        name: Checkout code

      - name: Generate files
        run: go generate ./...

      - name: Run tests
        run: go test ./...
